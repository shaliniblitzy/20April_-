# =============================================================================
# Runtime dependencies pinned per AAP §0.5.1.
#
#   Flask 3.1.3      — WSGI web framework (latest stable; requires Python >=3.9)
#   gunicorn 26.0.0  — production WSGI HTTP server (latest stable; requires Python >=3.10)
#   python-dotenv    — .env file loading for the flask CLI and runtime configuration
#
# Transitive dependencies (Werkzeug, Jinja2, MarkupSafe, ItsDangerous, Click,
# Blinker) are resolved by pip from Flask's own constraints; they are not
# pinned here so security patches are picked up automatically within Flask's
# supported ranges.
# =============================================================================
Flask==3.1.3
gunicorn==26.0.0
python-dotenv>=1.0.1
